using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WiggleWalk : MonoBehaviour
{
    //Just another empty script to hold onto things. It used to process body wiggling but that died when I moved from frames to time.
}
