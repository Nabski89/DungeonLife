using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Area : MonoBehaviour
{
    public string AreaType = "wood";
    // Start is called before the first frame update
    void Awake()
    {
     //   Destroy(gameObject, 0.1f);
    }
}
